<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="@dimen/form_width"
    android:layout_height="wrap_content">

    <RelativeLayout
        android:layout_height="@dimen/function_area_height"
        android:layout_gravity="center_vertical"
        android:layout_width="match_parent">

        <EditText
            android:id="@+id/search_contact_name_input"
            android:layout_alignParentLeft="true"
            android:layout_height="match_parent"
            android:layout_width="@dimen/contact_search_view_width"
            android:inputType="text"
            android:hint="@string/input_username"/>

        <Button
            android:background="@android:drawable/ic_search_category_default"
            android:layout_alignParentRight="true"
            android:layout_height="@dimen/form_button_logo_size"
            android:layout_width="@dimen/form_button_logo_size"
            android:onClick="doSearch"/>

        </RelativeLayout>

    <ListView
        android:id="@+id/contact_search_result"
        android:layout_marginTop="@dimen/contact_search_result_margin_top"
        android:visibility="gone"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"/>

</LinearLayout>